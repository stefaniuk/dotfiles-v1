#### httpd

##### service

    httpd -k start
    httpd -k restart
    httpd -k stop

##### info

    httpd -V    # configuration
    httpd -M    # modules
    httpd -S    # virtual hosts
    suexec -V   # suexec settings
